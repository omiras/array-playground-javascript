/**
 * Concatenar arrays
 */

const array1 = [3, 4, 5];
const array2 = [1, 2, 3];

/**
 * Modifica la asignación para concatenar los dos array según el orden natural de los números [1,2,3,3,4,5]; Clic para un ejemplo: https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Global_Objects/Array/concat
 */

let arrayResultado = array2.concat(array1); // <--- MODIFICAR
console.log(arrayResultado);